<p id="notice"><%= notice %></p>
<div class="pull-right">
  <h4>
  <%= link_to 'See all Transactions', transactions_path %> | 
  <%= link_to 'Transfer money', new_transaction_path %>
  </h4>
</div>
<h3>Accounts</h3>
<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Balance</th>
      <th>User</th>
      <th colspan="3">
        Actions
      </th>
    </tr>
  </thead>
  <tbody>
      <tr>
        <td><%= @accounts.account_holder_name %></td>
        <td><%= @accounts.account_number %></td>
        <td><%= @accounts.account_type %></td>
        <td>Rs. <%= @accounts.amount %></td>
        <td><%= link_to 'Show', @accounts %></td>
        <!-- <td><%#= link_to 'Edit', edit_account_path(account) %></td>
        <td><%#= link_to 'Destroy', account, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
  </tbody>
</table>
<br>